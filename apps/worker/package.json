{
  "name": "@repo/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "import": "tsx src/runHistoricalImport.ts",
    "hash-password": "tsx src/hashPassword.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@repo/core": "*",
    "@repo/db": "*",
    "dotenv": "^16.4.7",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
